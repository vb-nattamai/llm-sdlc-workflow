openai>=1.0.0
pydantic>=2.0.0
rich>=13.0.0
anyio>=4.0.0
pyyaml>=6.0.0
httpx>=0.27.0

# Note: Cypress e2e tests require Node.js + npm.
# After the pipeline runs, cd artifacts/<run>/generated && npm install cypress
# to enable automatic Cypress execution. The pipeline writes .cy.ts spec files
# regardless, so you can run them manually at any time.
